spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auctoritas
    username: auctoritas
    password: auctoritas_dev
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2

    jpa:
      show-sql: true
      properties:
        hibernate:
          format_sql: true

logging:
  level:
    dev.auctoritas: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
